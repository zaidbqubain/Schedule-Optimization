on.exit(dbDisconect(mydb))
args <- commandArgs(TRUE)
N <- args[1]
library(RMySQL)
drv <- dbDriver("MySQL")
con <- dbConnect(drv, username="g1117493", password="Group12", dbname ="g1117493", host="mydb.ics.purdue.edu")
querystatment <- "select
assignment_id,startdate,enddate,duration from
((students inner join roster on students.id = roster.student_id)
inner join courses on courses.id = class_id) inner join assignments on courses.symbol = assignments.classname WHERE student_id ="
library(DBI)
library(RMySQL)
library(RJDBC)
args <- commandArgs(TRUE)
N <- args[1]
?expect_equal
??expect_equal
1:5
4:2
?tapply
event
# Tell R that you are passing in arguments from an external source
library(RMySQL)
library(DBI)
args <- commandArgs(TRUE) # only need to run once for the entire file
